export const environment = {
  production: true,
  // Placeholder — updated once the Azure App Service is created and its real
  // URL is known. Until then, don't build with --configuration production.
  apiUrl: 'https://REPLACE_WITH_YOUR_APP_SERVICE_NAME.azurewebsites.net/api'
};
